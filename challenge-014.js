// 8kyu Find the Smallest integer in array
// had to look at solution
// confused on what to do with the class? and don't see an array at all in the provided starter code:

class SmallestIntegerFinder {
	  findSmallestInt(args) {
	  
	  }
	}
	
// solution is:

class SmallestIntegerFinder {
	  findSmallestInt(args) {
		return Math.min(...args)
	  }
	}
	
// I tried Math.min, but did not add 'return or the (...)