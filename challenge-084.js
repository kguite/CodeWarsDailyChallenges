//8kyu Area or Perimeter


const areaOrPerimeter = function(l , w) {
	  if (l === w) {
		return l*w
	  } else {
		return 2 * (l+w)
	  }
	};
	
	// OR
	
	
const areaOrPerimeter = function(l , w) {
	  if (l===w) return l*w
	  return 2 * (l+w)
	};