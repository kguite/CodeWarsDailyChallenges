// 8kyu Simple Fun #1: Seats in Theater

function seatsInTheater(nCols, nRows, col, row) {
	  //coding and coding..
	return (nCols - col + 1) * (nRows - row) //total number of seats in theater, 176, minus the columns the person is in, plus the column they're actually in)
	
	  
	}